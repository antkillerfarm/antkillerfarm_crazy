# Compile

`autoreconf -fi`

`./configure`

`make`

# readpipe.c & writepipe.c

the demo for naming pipe.

# filelock.c

the demo for file lock.

1.Open a terminal, and run `filelock`.

2.During 20s after Step 1, open another terminal, and run `filelock`.

# msgrecv.c & msgsend.c

the demo for message queue.

